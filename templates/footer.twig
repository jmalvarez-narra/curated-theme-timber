<div class="container">
  <div class="row">
    <div class="col-md-4 col-12 order-2 order-md-1 text-md-start text-center">
      <ul>
        {% for item in footer_menu_left.items %}
            <li {{ item.classes|join(' ') }}">
                <a class="nav-main-link" href="{{ item.link }}">{{ item.title }}</a>
                {% if item.children %}
                    <ul class="nav-drop">
                        {% for child in item.children %}
                            <li class="nav-drop-item">
                                <a href="{{ child.link }}">{{ child.title }}</a>
                            </li>
                        {% endfor %}
                    </ul>
                {% endif %}
            </li>
        {% endfor %}
      </ul>
    </div>

    <div class="col-md-4 col-12 order-1 order-md-2 text-center d-flex justify-content-center flex-column">
      <div class="logo">
        <img src="{{ site.theme.link }}/assets/img/curated_vintage_cars-dark.png"></img>
      </div>

      <ul class="icons d-flex justify-content-center">
        <li class="nav-item">
          <a class="nav-link" href="#">
            <img class="icon" src="{{ site.theme.link }}/assets/img/youtube.svg">
          </a>
        </li>

        <li class="nav-item">
          <a class="nav-link" href="#">
            <img class="icon" src="{{ site.theme.link }}/assets/img/instagram.svg">
          </a>
        </li>

        <li class="nav-item">
          <a class="nav-link" href="#">
            <img class="icon" src="{{ site.theme.link }}/assets/img/envelope-regular.svg">
          </a>
        </li>
      </ul>
    </div>

     <div class="col-md-4 col-12 order-3 text-md-end text-center">
      <ul>
        {% for item in footer_menu_right.items %}
            <li {{ item.classes|join(' ') }}">
                <a class="nav-main-link" href="{{ item.link }}">{{ item.title }}</a>
                {% if item.children %}
                    <ul class="nav-drop">
                        {% for child in item.children %}
                            <li class="nav-drop-item">
                                <a href="{{ child.link }}">{{ child.title }}</a>
                            </li>
                        {% endfor %}
                    </ul>
                {% endif %}
            </li>
        {% endfor %}
      </ul>
    </div>
  </div>

  <div class="divider"></div>

  <div class="row fs-7">
    <h6 class="col-md-4 text-md-start text-center">© 2022 CURATED. | ALL RIGHTS RESERVE</h6>
    <h6 class="col-md-4 text-center">2010 NE 2ND AVE MIAMI FL 33137</h6>
    <h6 class="col-md-4 text-md-end text-center">WEARECURATED.COM</h6>
  </div>
</div>

<script src="{{ site.theme.link }}/assets/js/bootstrap.bundle.min.js"></script>
<script>
  let latest = document.querySelectorAll('.multi-carousel-latest .carousel-item');
  let journal = document.querySelectorAll('.multi-carousel-journal .carousel-item');

  // homepage latest multi-carousel
  latest.forEach((el) => {
      const minPerSlide = 4
      let next = el.nextElementSibling
      for (var i=1; i<minPerSlide; i++) {
          if (!next) next = latest[0];

          let cloneChild = next.cloneNode(true);

          el.appendChild(cloneChild.children[0]);
          next = next.nextElementSibling;
      }
  });

  // homepage journal multi-carousel
  journal.forEach((el) => {
      const minPerSlide = 4
      let next = el.nextElementSibling
      for (var i=1; i<minPerSlide; i++) {
          if (!next) next = journal[0];

          let cloneChild = next.cloneNode(true);

          el.appendChild(cloneChild.children[0]);
          next = next.nextElementSibling;
      }
  });
</script>
